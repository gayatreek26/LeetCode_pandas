class Solution:
    def missingNumber(self, arr: list[int]) -> int:
        n = len(arr)

        # Common difference
        diff = (arr[-1] - arr[0]) // n

        for i in range(n - 1):
            if arr[i + 1] - arr[i] != diff:
                return arr[i] + diff

        return arr[0]
